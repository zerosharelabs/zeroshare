# Default HTTP configuration (before SSL)
server {
    listen 80;
    server_name _;
    # Allow Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
        try_files $uri =404;
    }
}
